<!-- File: MainWindow.xaml -->
<Window x:Class="todo_list.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:todo_list.Views"
        Title="ToDo Application" Height="auto" Width="auto" Background="#2c2966">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="800"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar for Categories -->
        <Border Background="#232152" Grid.Column="0">
            <views:CategoryListView />
        </Border>

        <!-- Main Task View -->
        <Border Background="#1a193d" Grid.Column="1">
            <views:TaskListView />
        </Border>
    </Grid>
</Window>
